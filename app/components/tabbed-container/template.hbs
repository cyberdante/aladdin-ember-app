{{#paper-card class="aladdin-tabs-container" as |card|}}
    {{#card.content}}
        {{#paper-tabs
            class="aladdin-tab"
            selected=selectedTab
            borderBottom="true"
            shouldPaginate=false
            onChange=(action 'onChange') as |tabs|}}
            {{#each tabTitles as |title|}}
                {{#tabs.tab}}
                    {{title}}
                {{/tabs.tab}}
            {{/each}}
        {{/paper-tabs}}
        {{#if (not-eq selectedComponent 'contract-viewer')}}
            {{component selectedComponent showTitle=false schema=schema onViewChange=(action onViewChange)}}
        {{else}}
            {{component selectedComponent showTitle=false contractType='ethereum' value=code onViewChange=(action onViewChange)}}
        {{/if}}
    {{/card.content}}
{{/paper-card}}
